[general]
accounts = fiit
pythonfile = ~/dotfiles/gnome-keyring.py
ui = quiet
fsync = False

[Account fiit]
localrepository = Local
remoterepository = Remote
status_backend = sqlite

[Repository Local]
type = Maildir
localfolders = ~/.mail/fiit
folderfilter = lambda folder: folder not in ['.mailboxlist', 'mailboxlist', 'Trash']

[Repository Remote]
type = IMAP
remotehost = dcs.fiit.stuba.sk
# Set the username & password by running ~/dotfiles/gnome-keyring.py
remoteusereval = get_username('dcs.fiit.stuba.sk')
remotepasseval = get_password('dcs.fiit.stuba.sk')
maxconnections = 1
folderfilter = lambda folder: folder not in ['.mailboxlist', 'mailboxlist', 'Trash', 'Archive', 'fiit', 'mbox']

[mbnames]
enabled = no
filename = ~/.mutt/mailboxes
header = "mailboxes "
peritem = "+%(accountname)s/%(foldername)s"
sep = " "
footer = "\n"
