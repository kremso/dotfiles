# Use the internal CCID driver of scdaemon. On macOS, the built-in CCID
# driver conflicts with the system smartcard service; disable it so
# scdaemon uses pcsc-lite instead. Required for Yubikey to work reliably.
disable-ccid
